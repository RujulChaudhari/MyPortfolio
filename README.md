# Rujul Chaudhari — Portfolio (Updated)

Tech: React + Vite + TailwindCSS + Framer Motion + tsParticles

## Local setup (VSCode)
1. npm install
2. npm run dev
3. Open http://localhost:5173

Notes:
- If Tailwind or PostCSS errors appear, ensure `@tailwindcss/postcss` is installed and postcss config uses it (postcss.config.cjs provided).
- This project uses `react-tsparticles` for interactive background particles. Install deps with npm.
